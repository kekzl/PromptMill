# PromptMill Environment Configuration
# Copy this file to .env and modify as needed

# Server Configuration
# Default: 127.0.0.1 (localhost only, secure)
# Use 0.0.0.0 to expose to network (requires reverse proxy for production)
SERVER_HOST=127.0.0.1
SERVER_PORT=7610

# Models Directory
# Where downloaded LLM models are stored
MODELS_DIR=/app/models

# Logging Level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
